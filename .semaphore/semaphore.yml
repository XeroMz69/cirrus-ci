version: v1.0
name: Docker
agent:
  machine:
    type: e1-standard-8
    os_image: ubuntu:22.04
blocks:
  - name: Build
    task:
      jobs:
        - name: docker build
          commands:
            - checkout
            - df -h
            - export DEBIAN_FRONTEND=noninteractive
  - name: 'Block #2'
    task:
      jobs:
        - name: 'Job #1'
          commands:
            - checkout
  - name: 'Block #3'
    task:
      jobs:
        - name: 'Job #1'
          commands: []
